name: CI Deploy

on:
  push:
    branches:
      - main

permissions:
  write: write
  workflows: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Deploy
        run: echo 'Deploying...'